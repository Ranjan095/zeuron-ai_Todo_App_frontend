let initialState = {
  tasks: [],
  isLoading: false,
};

let taskReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default taskReducer;
