let initialState = {
  isAuth: false,
  isLoading: false,
};

let authReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default authReducer;
